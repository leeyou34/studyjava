package sec06.exam04.package2;

import sec06.exam05.package1.A;

public class C {
	//field
	A a1 = new A(/*true*/);
	//A a2 = new A(1); // default constructor can't access
	//A a3 = new A("string"); // private constructor can't access
}
