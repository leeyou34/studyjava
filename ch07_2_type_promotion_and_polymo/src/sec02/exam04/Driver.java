package sec02.exam04;

	public class Driver {// Driver class it's object.
		//method
		public void drive(Vehicle vehicle) { // no return value since void has declared. there is parameter and its'given. and Vehicle vehicle is child object
			vehicle.run(); // that parameter Vehicle vehicle would do this method 'run(); and this vehicle child object restated the run method
		}
	}
